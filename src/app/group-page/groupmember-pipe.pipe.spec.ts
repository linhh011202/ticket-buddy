import { GroupmemberPipePipe } from './groupmember-pipe.pipe';

describe('GroupmemberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GroupmemberPipePipe();
    expect(pipe).toBeTruthy();
  });
});
